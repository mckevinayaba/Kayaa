import type { Venue, Event, Post, CheckIn } from '../types';

// All seed/mock data has been removed.
// These empty exports exist only to avoid breaking any lingering imports
// while the codebase is fully cleaned up. They will never surface in the product.

export const mockVenues: Venue[] = [];
export const mockEvents: Event[] = [];
export const mockPosts: Post[] = [];
export const mockCheckIns: CheckIn[] = [];
